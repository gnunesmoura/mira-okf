---
type: Command
title: props
description: Project frontmatter properties from OKF concepts.
tags: [command, props]
---

# `props`

```text
tooling okf props docs [--field <name>]... [--json]
```

With no fields, the command reports available properties. Repeat `--field` to
select specific names; selecting the same field twice is invalid.
