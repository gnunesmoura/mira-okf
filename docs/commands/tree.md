---
type: Command
title: tree
description: Summarize an OKF bundle tree.
tags: [command, tree]
---

# `tree`

```text
mira-okf okf tree docs --depth <n> [--summary] [--json]
```

`--depth` defaults to `2`; `--summary` adds concept and reserved-file counts.
The command is read-only and returns a compact directory view.
